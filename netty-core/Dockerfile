FROM eclipse-temurin:23-jdk

WORKDIR /app
COPY . .

# Build with Gradle (assumes build.gradle exists)
# RUN ./gradlew shadowJar

EXPOSE 25565 8080
# CMD ["java", "-jar", "build/libs/netty-runtime.jar"]
CMD ["echo", "Build step needed — see build.gradle"]
